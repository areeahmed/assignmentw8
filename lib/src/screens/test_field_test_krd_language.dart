import 'package:flutter/material.dart';

class TestingTextField extends StatelessWidget {
  const TestingTextField({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: TextField(),
      ),
    );
  }
}
